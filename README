This is the Meteorological Post-Processing Package (MPPP). 

The documentation is in the `doc` directory and can be build with sphinx (it
has to be installed) using::

   make html

or::

   make latex

from there.